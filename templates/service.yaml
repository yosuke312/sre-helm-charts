apiVersion: v1
kind: Service
metadata:
  name: {{ include "foca-webhook-chart.fullname" . }}
  labels:
    {{- include "foca-webhook-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foca-webhook-chart.selectorLabels" . | nindent 4 }}
